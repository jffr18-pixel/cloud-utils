# ─── Dependencias principales (se instalan en cualquier sistema) ───
anthropic>=0.45.0
streamlit>=1.30.0
pillow>=10.0.0
python-docx>=1.1.0
fpdf2>=2.7.6
pillow-heif>=0.13.0
openpyxl>=3.1.0
altair>=5.0.0
pandas>=2.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# ─── Lectura de PDF ───
pdfplumber>=0.11.0          # texto de PDFs digitales
pymupdf>=1.24.0             # texto + conversion de PDFs escaneados (sin programas extra)
pdf2image>=1.17.0           # alternativa para PDFs escaneados (requiere Poppler)

# ─── Motor OCR (opcional pero recomendado) ───
# RapidOCR no necesita instalar ningun programa aparte, pero depende de
# onnxruntime, que solo tiene versiones para Python 3.8–3.12 (64 bits).
# El marcador de abajo lo instala solo si tu Python es compatible; si no,
# instala Tesseract (ver README) o usa solo la lectura de PDFs digitales.
pytesseract>=0.3.10
rapidocr-onnxruntime>=1.3.0; python_version >= "3.8" and python_version < "3.13"
